//# ifndef __FILTER_H
//# define __FILTER_H

//#include "main.h" 
//typedef float fp32;

//typedef struct
//{
//  uint32_t numStages;      /**< number of 2nd order stages in the filter.  Overall order is 2*numStages. */
//	fp32 *pState; /**< Points to the array of state coefficients.  The array is of length 4*numStages. */
// const fp32 *pCoeffs; /**< Points to the array of coefficients.  The array is of length 5*numStages */
//} arm_biquad_casd_df1_inst_f32;

//# endif

//void arm_biquad_cascade_df1_f32(
//  const arm_biquad_casd_df1_inst_f32 * S,
//  float * pSrc,
//  float * pDst,
//  unsigned int blockSize);

//void arm_biquad_cascade_df1_init_f32(
//        arm_biquad_casd_df1_inst_f32 * S,
//        uint8_t numStages,
//  const fp32* pCoeffs,
//        fp32 * pState);
//	
